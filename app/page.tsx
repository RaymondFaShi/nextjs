'use client'

// react
import React from "react";

// interface

export default function RootPage() {
    log( '1', 2, 3 );
    React.useEffect( () => {

    }, [] );

    return (
        <>This is root</>
    );
}