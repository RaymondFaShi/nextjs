'use client'

// react
import React from "react";

// interface
export default function RootPage() {
    // construct
    React.useEffect( () => {
        // log( 111 );
    } );
    return (
        <b>111</b>
    );
}