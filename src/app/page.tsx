'use client'

// react
import React from "react";

// interface
export default function RootPage() {
    console.log( typeof log );
    console.warn( 111 );

    // construct
    React.useEffect( () => {
        // console.log( typeof log );
    } );
    return (
        <b>111</b>
    );
}